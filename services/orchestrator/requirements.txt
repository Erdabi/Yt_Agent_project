-r ../../requirements/base.txt

fastapi==0.115.5
uvicorn[standard]==0.32.1

# Manager Agent's reasoning engine (app/manager/reasoning.py) calls the
# configured `llm` provider (libs.providers.get_provider("llm")) — Ollama
# by default (libs/providers/llm/ollama_provider.py, `requests`-based, no
# extra dependency beyond the one below); anthropic remains available as
# a swappable paid-vendor alternative (config/providers.yaml). Also
# needed by the manager_worker Compose service, which reuses this same
# image.
requests==2.32.3
anthropic==0.120.0
