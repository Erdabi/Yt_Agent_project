-r ../../requirements/base.txt

# The Research Agent's idea/knowledge generation (app/idea_generator.py,
# app/knowledge_builder.py) call the configured `llm` provider
# (libs.providers.get_provider("llm")) — Ollama by default
# (libs/providers/llm/ollama_provider.py, `requests`-based, no extra
# dependency beyond the one below); anthropic remains available as a
# swappable paid-vendor alternative (config/providers.yaml).
requests==2.32.3
anthropic==0.120.0
