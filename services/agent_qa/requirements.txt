-r ../../requirements/base.txt

# The Quality Control Agent's reviewers (app/reviewers/) call the
# configured `llm` provider (libs/providers/llm/), whose real
# implementation (libs/providers/llm/anthropic_provider.py) calls the
# Claude API directly, same as the Research/Script Agents' own LLM calls.
anthropic==0.120.0
