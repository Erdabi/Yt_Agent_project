-r ../../requirements/base.txt

# The Script Agent's generation call (app/script_generator.py) calls the
# Claude API directly, same as the Manager Agent's reasoning engine and
# the Research Agent's idea generation.
anthropic==0.120.0
