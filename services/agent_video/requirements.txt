-r ../../requirements/base.txt

# No real-vendor-specific dependencies yet. Each module will add its own
# when a real provider lands behind it (docs/architecture/06-roadmap.md):
# asset_generation.py (stock-media/image-gen/video-gen/audio-library client
# libraries), voice_generation.py (TTS provider SDK), rendering.py
# (ffmpeg-python/moviepy, plus the `ffmpeg` binary itself), thumbnail.py
# (image-gen client library) — until then this image only needs the shared
# base requirements.
