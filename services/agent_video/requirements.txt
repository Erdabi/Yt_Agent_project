-r ../../requirements/base.txt

# No agent-specific dependencies yet. Each module below will add its own
# real dependency when its actual logic lands (docs/architecture/06-roadmap.md):
# storyboard.py (stock-media/image-gen client libraries), voiceover.py (TTS
# provider SDK), assembly.py (ffmpeg-python/moviepy), thumbnail.py
# (image-gen client library) — until then this image only needs the shared
# base requirements.
